{
  "name": "@extend-chrome/storage",
  "version": "0.2.1",
  "description": "A wrapper for the Chrome Extension Storage API that adds promises and functional set transactions similar to the React `this.setState` API.",
  "repository": "extend-chrome/storage",
  "license": "MIT",
  "author": "<jacksteamdev@gmail.com>",
  "files": [
    "lib",
    "index.d.ts"
  ],
  "main": "lib/index-cjs.js",
  "module": "lib/index-esm.js",
  "scripts": {
    "build": "rollup -c",
    "start": "rollup -c -w",
    "test": "jest",
    "preversion": "npm run build",
    "postversion": "git push && npm publish"
  },
  "jest": {
    "setupFilesAfterEnv": [
      "./tests/jest.setup.js"
    ]
  },
  "dependencies": {
    "@extend-chrome/events-rxjs": "^0.12.1",
    "@types/chrome": "0.0.122",
    "chrome-promise": "^3.0.5",
    "rxjs": "^6.5.2"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.5.5",
    "eslint": "^5.16.0",
    "eslint-plugin-jsdoc": "^4.8.4",
    "jest": "^24.8.0",
    "npm-run-all": "^4.1.5",
    "power-assert": "^1.6.1",
    "rollup": "^0.68.0",
    "sinon-chrome": "^2.3.2"
  }
}
